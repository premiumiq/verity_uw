"""JSON REST API surface for Verity.

Thin wrappers over the Verity SDK facade (`verity.registry.*`,
`verity.lifecycle.*`, `verity.execution.*`, `verity.decisions.*`,
`verity.reporting.*`), exposed under `/api/v1/*`.

The admin HTML UI continues to live at `/admin/*` and is unrelated to
anything in this package.
"""
